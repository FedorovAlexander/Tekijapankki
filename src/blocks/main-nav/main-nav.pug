mixin main-nav(selected)

  -
    var menu = {
      'Home': '/home',
      'Blog': '/blog',
      'Contact': '/contact'
    };

  nav.main-nav
    ul.main-nav__list
      each val, key in menu
        li
          a(href=val, title=key)= key
