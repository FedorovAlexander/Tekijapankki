mixin main-nav(selected)

  -
    var menu = {
      'Tekijät': '/painters/',
      'Tietoa palvelusta': '/about/'
    };

  nav.main-nav
    ul.main-nav__list
      li.main-nav__list-item
        a.main-nav__list-link(href='/', title='Home')
          svg(width="18", height="18")
            use(xlink:href="img/sprite-svg.svg#home")
      each val, key in menu
        li.main-nav__list-item
          a.main-nav__list-link(href=val, title=key)= key
